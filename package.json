{
  "name": "@pdubz/clockify-mcp",
  "version": "2.0.0",
  "description": "MCP server for Clockify — manage time entries, projects, tags, and generate reports",
  "bin": { "clockify-mcp": "./dist/index.js" },
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "tsx --test test/reports.test.ts"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.7",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "zod": "^3.24.2"
  }
}
